cd y busco apache-tomcat-9.0.80/bin
chmod +x *.sh

nano ~/.bashrc
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export PATH=$JAVA_HOME/bin:$PATH
Guardá con Ctrl+O, luego Enter, y salí con Ctrl+X.
source ~/.bashrc
echo $JAVA_HOME

./startup.sh